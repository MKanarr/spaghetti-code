#include <iostream>

void print(char world) {
   int i = 0;  while (filename[i] != '\0') { cout << filename[i]; i++; }
}
int main() {
    char world[] = "World!";

    // my comment
    std::cout << "Hello UTD" << std::endl;

    print(filename);
    return 0;
}